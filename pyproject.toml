[project]
name = "citibike-scraper"
version = "0.1.0"
description = "Citi Bike station availability scraper and forecaster"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.10",
    "httpx>=0.28.1",
    "psycopg[binary]>=3.3.4",
    "structlog>=25.5.0",
]
